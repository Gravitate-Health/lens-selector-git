apiVersion: v1
kind: Service
metadata:
  name: {{ include "lens-selector.fullname" . }}
  labels:
    {{- include "lens-selector.labels" . | nindent 4 }}
    {{- with .Values.service.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lens-selector.selectorLabels" . | nindent 4 }}
