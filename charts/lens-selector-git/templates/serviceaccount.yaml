apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "lens-selector-git.serviceAccountName" . }}
  labels:
    {{- include "lens-selector-git.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
