apiVersion: v1
kind: Service
metadata:
  name: {{ include "lens-selector-git.fullname" . }}
  labels:
    {{- include "lens-selector-git.labels" . | nindent 4 }}
    {{- with .Values.service.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lens-selector-git.selectorLabels" . | nindent 4 }}
